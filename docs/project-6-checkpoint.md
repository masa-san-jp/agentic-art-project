# Project #6 receiver checkpoint

CODE_IMPLEMENTED / ACCEPTANCE_BLOCKED. Branch agent/project-6-attestation-receiver starts at the isolated
existing PR9 candidate 55615f985d3376cafd5c9c08ecdc579be1c8641d, tree
647af8065845f5aa522c62eb13daa286a0df54a9. This is not current main and is not a
qualified implementation of the revised Issue #6. No existing branch is reset.

Current Issue #6 requires Production attestation/v1 and projection/v2; PR9 has
heading checks and v1, and retains summaries in the public catalog. Implement the
receiver contract in code with synthetic fixture evidence. Actual P0001..P0007
migration/deletion is explicitly not authorized by the AAK start instruction;
do not alter existing record bodies or generated public catalogs to hide this
remaining gate. A read-only migration manifest will make that final action
reviewable. AAK-13 cannot claim dependency acceptance before it is resolved.

Required checks: python3 tools/validate.py --check, python3 tools/catalog_sync.py
--check, python3 -m unittest discover -s tests -v, git diff --check. Existing-data
failures must remain visible, separately from synthetic receiver code checks.

Local code 9c137342352725e61cf09c293b1d222cc7ad34f6 and remote candidate
30a862b085ccce76971e9a98af92343cefb516e5 share exact tree
e0bce3dcdb97dffa92e6918ac56954a14be5e699, verified blob by blob and as a full tree.
All current public record bodies are preserved. The new fixture is explicitly
synthetic and was generated by the actual Production owner renderer/attestation
at local 65a58acb2e07f9bf436777e77343739b332fe1e1 (remote tree-equivalent candidate
d323b92ffef34fc80b2e0c47daa1b8acff40368a). Its public body SHA-256 is
aa4a5dc01396d62a1c74f8a9c7b7a926f457ca2d10b3aa97b6218adf337b556f.
Copied body, attestation and both assets were byte-compared with the owner output.

Synthetic receiver tests: 6 PASS (exact bytes, summary/heading imitation/tamper,
missing/forged attestation, revision/provenance, asset failures, unknown rights).
Full suite: 18 tests, 16 PASS, 1 FAIL and 1 ERROR. Both failures are current-data
gates: test_repository_is_valid and test_public_plan_index_is_rendered_in_both_catalogs.
Native validator and catalog --check FAIL at P0001 because blocked legacy records
cannot enter the canonical-only index. git diff --check PASS. These failures are
not waived and Project #6 is not a qualified dependency for AAK-13.

Read-only migration classification: P0001, P0002, P0003, P0004, P0005, P0006 and
P0007 all MIGRATION_REQUIRED; none has a current verified attestation. Exact source
candidates and blocking reasons are emitted by python3 tools/migration_plan.py.
Reviewable action: for each reserved P ID, recover Production-verified exact
body/assets/attestation with matching stable identity, or authorize removal of the
current record directory and index listing while reserving only id/source_candidate/
blocking_reason/unblock_condition in plans/migration.yaml. Do not copy old summary
text to another public filename. Run catalog_sync.py --write only after that
authorized classification is applied; then rerun all three native gates.

Projection/v2 integration with Orchestration #193 is NOT_RUN; receiver metadata
fixture is synthetic. No stable identity, creator or artistic provenance has been
inferred for the legacy records. The old PR9 is retained; this change is stacked
on its exact candidate for review and does not modify that branch.

No private/raw, real viewer response, profile, internal handoff or conversation is
added. Publication, merge, release and actual data migration remain human-gated.
